# ==================================
# Development Environment Config
# ==================================
# This inherits from root .envrc but can override values

# Load parent .envrc if it exists
source_up_if_exists

# Development-specific overrides (uncomment to customize)
export PROJECT_ID="gcp-training-329013"
export REGION="europe-west4"


# Ensure Terraform variables are set
export TF_VAR_gcp_project_id="$PROJECT_ID"
export TF_VAR_name="online-boutique-dev"
export TF_VAR_namespace="development"
export TF_VAR_gcp_project_id="${TF_VAR_gcp_project_id:-$PROJECT_ID}"
export TF_VAR_region="${TF_VAR_region:-$REGION}"

gcloud config set project $PROJECT_ID
gcloud config set compute/region $REGION

echo "🛠️ Development environment active: $TF_VAR_gcp_project_id"
